require "spec_helper"

describe "get /api/messages", :type => :api do

  # it "provides a valid response when getting a message" do
  #   message = Message.create(body: "body")
  #   url = "/api/v1/messages.json"
  #   get "#{url}"
  #   last_response.status.should == 200
  # end
end