# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant::Config.run do |config|
  config.ssh.username = "deployer"
  config.vm.provision :shell, :path => "config/setup_vagrant.sh"
  config.vm.box = "development"
end
