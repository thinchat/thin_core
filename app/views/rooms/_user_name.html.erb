<% if @user.class == Guest %>
  <%= form_for @user, :remote => true do |f| %>
    <%= f.label :user_name %>
    <%= f.text_field :name %>

    <%= f.label :email %>
    <%= f.text_field :email %>
    <%= f.submit "Update" %>
  <% end %>
<% end %>

<%= @session.inspect %>