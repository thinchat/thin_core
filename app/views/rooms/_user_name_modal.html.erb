<div class="modal hide" id="userModal">
  <div class="modal-header">
    <h3>Welcome to <span>thin</span>Chat!</h3>
  </div>
  <div class="modal-body">
    <p>Howdy! Great to have you. Here's some tips for getting the most out of your chat session</p>

    <ul>
      <li>You can <strong>drag and drop files</strong> into the window to share with the agent.</li>
      <li><strong>Paste image links</strong> to see them appear in the chat.</li>
      <li>You can receive an <strong>email transcript</strong> at the end your chat session.</li>
    </ul>
    <br>
    <h3>If you like, set your display name and email address below.</h3>
    <br>
      <%= form_for current_user, :html => { :class => "form-inline"}, :remote => true do |f| %>
        <%= f.label :display_name %>
        <%= f.text_field :name, :placeholder => "Display name" %>
        <%= hidden_field_tag 'room_id', @room.id %>
        <br><br>
        <%= f.label :email_address %>
        <%= f.text_field :email, :placeholder => "Email Address" %>
 </div>
        <div class="modal-footer">
        <%= f.submit "Submit", :class => "btn btn-primary", :id => "user_info_update"  %>
        <a href="#" class="btn" data-dismiss="modal">No Thanks</a>
      </div>

      
      <% end %>
 
</div>