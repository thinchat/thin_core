<div class="navbar navbar-fixed-top <%= current_path_check(root_url) %>">
  <div class="navbar-inner">
    <div class="container-fluid">
      <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <a class="brand" href="/">thin<span>Chat</span></a>
      <div class="container nav-collapse">
        
        <% if current_user.agent? %>
        <ul class="nav">
          <li><%= link_to "Lobby", rooms_path() %></li>
          <li><%= link_to "Online Agents", agents_path() %></li>
          <li class="dropdown"><%= render "layouts/nav_login" %></li>
        </ul>

          <form class="navbar-search pull-right" action="<%= find_path %>">
            <input type="text" name="query" class="search-query" placeholder="Search">
          </form>

        <% else %>
        <ul class="nav pull-right">
          <li><%= link_to "Agent login", "/auth" %></li>
        </ul>
        <% end %>

      <%= content_tag "div", id: "user", data: {user: current_user.user_hash} do %>
      <% end %>
      </div><!--/.nav-collapse -->
    </div>
  </div>
</div>